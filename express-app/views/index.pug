extends layout

block content
  each post in posts
    div(class='mt-3 mb-2 p-2 border')
      h3 #{post.title}
      div !{post.content}
      div(class='post-footer mt-1') Posted at #{post.postedStr} by #{post.author}
